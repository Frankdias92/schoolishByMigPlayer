function Loading() {
  return (
    <div
      style={{
        display: "flex",
        width: "100vw",
        height: "100vh",
        placeContent: "center",
        flexDirection: "column",
        alignContent: "center",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Loading...
    </div>
  );
}

export default Loading;
